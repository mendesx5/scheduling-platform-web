import { createContext,useContext,useMemo,useState,type ReactNode } from 'react';
function decode(token:string|null){ if(!token)return null; try{return JSON.parse(atob(token.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')))}catch{return null} }
type Ctx={tenantToken:string|null;platformToken:string|null;claims:any;platformClaims:any;setTenantToken:(v:string|null)=>void;setPlatformToken:(v:string|null)=>void;logout:()=>void;logoutPlatform:()=>void};
const AuthContext=createContext<Ctx>(null as any);
export function AuthProvider({children}:{children:ReactNode}){const [tenantToken,setTenantState]=useState(localStorage.getItem('tenant_token'));const [platformToken,setPlatformState]=useState(localStorage.getItem('platform_token'));const setTenantToken=(v:string|null)=>{v?localStorage.setItem('tenant_token',v):localStorage.removeItem('tenant_token');setTenantState(v)};const setPlatformToken=(v:string|null)=>{v?localStorage.setItem('platform_token',v):localStorage.removeItem('platform_token');setPlatformState(v)};const value=useMemo(()=>({tenantToken,platformToken,claims:decode(tenantToken),platformClaims:decode(platformToken),setTenantToken,setPlatformToken,logout:()=>setTenantToken(null),logoutPlatform:()=>setPlatformToken(null)}),[tenantToken,platformToken]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export const useAuth=()=>useContext(AuthContext);
