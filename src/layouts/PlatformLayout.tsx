import {Building2,LayoutDashboard,LogOut,Menu,X} from 'lucide-react';import {NavLink,Outlet} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';import {useState} from 'react';
export default function PlatformLayout(){const {logoutPlatform}=useAuth();const [open,setOpen]=useState(false);return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand">Agenda<span>Hub</span><small>Platform</small><button className="icon mobile" onClick={()=>setOpen(false)}><X/></button></div><nav><NavLink to="/platform" end onClick={()=>setOpen(false)}><LayoutDashboard size={19}/>Métricas</NavLink><NavLink to="/platform/tenants" onClick={()=>setOpen(false)}><Building2 size={19}/>Tenants</NavLink></nav><button className="logout" onClick={logoutPlatform}><LogOut size={18}/>Sair</button></aside><main className="main"><header className="topbar"><button className="icon mobile" onClick={()=>setOpen(true)}><Menu/></button><div><strong>Administração da plataforma</strong><small>Visão global do SaaS</small></div></header><div className="content"><Outlet/></div></main></div>}
