import {CalendarDays,LayoutDashboard,MapPin,Users,Clock,Settings,LogOut,Menu,X,CalendarRange,Globe2} from 'lucide-react';
import {NavLink,Outlet} from 'react-router-dom';
import {useState} from 'react';
import {useAuth} from '../contexts/AuthContext';
import {useI18n} from '../contexts/I18nContext';
export default function AdminLayout(){const [open,setOpen]=useState(false);const {logout}=useAuth();const {t}=useI18n();const nav=[['/app',t('nav.overview'),LayoutDashboard],['/app/bookings',t('nav.bookings'),CalendarDays],['/app/agenda',t('nav.agenda'),CalendarRange],['/app/venues',t('nav.venues'),MapPin],['/app/customers',t('nav.customers'),Users],['/app/team',t('nav.team'),Users],['/app/page',t('nav.page'),Globe2],['/app/settings',t('nav.settings'),Settings],['/app/subscription',t('nav.subscription'),Clock]] as const;const mobileNav=nav.slice(0,5);return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand">Agenda<span>Hub</span><button className="icon mobile" onClick={()=>setOpen(false)}><X/></button></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/app'} onClick={()=>setOpen(false)}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/>Sair</button></aside><main className="main"><header className="topbar"><button className="icon mobile" onClick={()=>setOpen(true)}><Menu/></button><div><strong>Painel do estabelecimento</strong><small>Gerencie sua operação e sua página pública</small></div></header><div className="content"><Outlet/></div></main><nav className="mobile-bottom">{mobileNav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/app'}><Icon/><span>{label}</span></NavLink>)}</nav></div>}
