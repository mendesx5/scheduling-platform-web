import {useEffect,useState} from 'react';import {customersApi} from '../../api/services';import type {Customer} from '../../types';import {Card,Empty} from '../../components/ui';
export default function CustomersPage(){const [items,setItems]=useState<Customer[]>([]);useEffect(()=>{customersApi.list().then(setItems)},[]);return <><div className="page-head"><div><h1>Clientes</h1><p>Base criada a partir das reservas.</p></div></div><Card>{!items.length?<Empty title="Nenhum cliente" description="Clientes aparecerão conforme fizerem reservas."/>:<div className="table-wrap"><table><thead><tr><th>Nome</th><th>Telefone</th><th>E-mail</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.phone}</td><td>{c.email||'—'}</td></tr>)}</tbody></table></div>}</Card></>}
