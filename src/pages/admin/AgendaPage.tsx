import {useEffect,useMemo,useState} from 'react';
import {bookingsApi,venuesApi} from '../../api/services';
import type {Booking,Venue} from '../../types';
import {Badge,Card,Empty} from '../../components/ui';
import {ChevronLeft,ChevronRight} from 'lucide-react';
const tone=(s:string)=>s==='CONFIRMED'||s==='COMPLETED'?'success':s==='PENDING'?'warning':s==='CANCELLED'?'danger':'neutral';
const dayKey=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
export default function AgendaPage(){const [bookings,setBookings]=useState<Booking[]>([]);const [venues,setVenues]=useState<Venue[]>([]);const [venue,setVenue]=useState('ALL');const [cursor,setCursor]=useState(new Date());const [selected,setSelected]=useState<string>('');useEffect(()=>{Promise.all([bookingsApi.list(),venuesApi.list()]).then(([b,v])=>{setBookings(b);setVenues(v)})},[]);const days=useMemo(()=>{const first=new Date(cursor.getFullYear(),cursor.getMonth(),1);const start=new Date(first);start.setDate(first.getDate()-first.getDay());return Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d})},[cursor]);const filtered=bookings.filter(b=>b.status!=='CANCELLED'&&(venue==='ALL'||String(b.venueId)===venue));const selectedItems=selected?filtered.filter(b=>dayKey(new Date(b.startDateTime))===selected):[];return <><div className="page-head"><div><h1>Agenda</h1><p>Visualize todo o calendário do mês e acompanhe suas reservas.</p></div><select value={venue} onChange={e=>setVenue(e.target.value)}><option value="ALL">Todos os espaços</option>{venues.map(v=><option value={v.id} key={v.id}>{v.name}</option>)}</select></div><Card><div className="calendar-toolbar"><button className="icon" onClick={()=>setCursor(new Date(cursor.getFullYear(),cursor.getMonth()-1,1))}><ChevronLeft/></button><strong>{cursor.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</strong><button className="icon" onClick={()=>setCursor(new Date(cursor.getFullYear(),cursor.getMonth()+1,1))}><ChevronRight/></button></div><div className="month-weekdays">{['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(x=><b key={x}>{x}</b>)}</div><div className="month-grid">{days.map(d=>{const key=dayKey(d),items=filtered.filter(b=>dayKey(new Date(b.startDateTime))===key);return <button key={key} onClick={()=>setSelected(key)} className={`month-day ${d.getMonth()!==cursor.getMonth()?'outside':''} ${selected===key?'selected':''}`}><span>{d.getDate()}</span>{items.slice(0,3).map(b=><small key={b.id} className={`dot ${b.status.toLowerCase()}`}>{new Date(b.startDateTime).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})} · {venues.find(v=>v.id===b.venueId)?.name||'Reserva'}</small>)}{items.length>3&&<em>+{items.length-3} reservas</em>}</button>})}</div></Card>{selected&&<Card className="agenda-day-details"><h2>{new Date(selected+'T12:00:00').toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'})}</h2>{selectedItems.length?<div className="stack">{selectedItems.sort((a,b)=>+new Date(a.startDateTime)-+new Date(b.startDateTime)).map(b=><div className="row-item" key={b.id}><span><strong>{venues.find(v=>v.id===b.venueId)?.name||'Espaço'}</strong><small>{new Date(b.startDateTime).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})} — {new Date(b.endDateTime).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</small></span><Badge tone={tone(b.status) as any}>{b.status}</Badge></div>)}</div>:<Empty title="Dia livre" description="Nenhuma reserva neste dia."/>}</Card>}</>}
