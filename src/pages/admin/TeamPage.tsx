import {useEffect,useState} from 'react';import {usersApi} from '../../api/services';import type {Role,User} from '../../types';import {Card,Field,Empty,Badge} from '../../components/ui';import {useAuth} from '../../contexts/AuthContext';
export default function TeamPage(){const [items,setItems]=useState<User[]>([]);const [form,setForm]=useState<{name:string;email:string;password:string;role:Role}>({name:'',email:'',password:'',role:'EMPLOYEE'});const [error,setError]=useState('');const {claims}=useAuth();const load=()=>usersApi.list().then(setItems).catch(()=>setItems([]));useEffect(()=>{load()},[]);const isOwner=claims?.role==='OWNER';return <><div className="page-head"><div><h1>Equipe</h1><p>Gerencie quem acessa o painel.</p></div></div><div className="split"><Card><h2>Novo usuário</h2><form onSubmit={async e=>{e.preventDefault();setError('');try{await usersApi.create(form);setForm({name:'',email:'',password:'',role:'EMPLOYEE'});load()}catch(e:any){setError(e.message)}}}><Field label="Nome"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="E-mail"><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Senha"><input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field><Field label="Permissão"><select value={form.role} onChange={e=>setForm({...form,role:e.target.value as Role})}><option value="EMPLOYEE">Funcionário</option>{isOwner&&<><option value="MANAGER">Gerente</option><option value="OWNER">Proprietário</option></>}</select></Field>{error&&<div className="alert danger">{error}</div>}<button className="btn primary full">Adicionar usuário</button></form></Card><Card><h2>Usuários</h2>{!items.length?<Empty title="Sem usuários visíveis" description="Sua permissão pode não permitir listar a equipe."/>:<div className="stack">{items.map(u=><div className="row-item" key={u.id}><span><strong>{u.name}</strong><small>{u.email}</small></span><Badge tone={u.role==='OWNER'?'success':'neutral'}>{u.role}</Badge></div>)}</div>}</Card></div></>}
