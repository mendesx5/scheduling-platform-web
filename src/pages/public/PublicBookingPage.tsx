import {useEffect,useMemo,useState} from 'react';import {useParams} from 'react-router-dom';import {publicApi} from '../../api/services';import type {Tenant,Venue} from '../../types';import {Field,money} from '../../components/ui';import {CalendarDays,Clock,MapPin,CheckCircle2} from 'lucide-react';
export default function PublicBookingPage(){const {slug=''}=useParams();const [tenant,setTenant]=useState<Tenant>();const [venues,setVenues]=useState<Venue[]>([]);const [venueId,setVenueId]=useState<number>();const [date,setDate]=useState('');const [slots,setSlots]=useState<string[]>([]);const [slot,setSlot]=useState('');const [form,setForm]=useState({customerName:'',customerPhone:'',customerEmail:''});const [done,setDone]=useState(false);const [error,setError]=useState('');useEffect(()=>{Promise.all([publicApi.tenant(slug),publicApi.venues(slug)]).then(([t,v])=>{setTenant(t);setVenues(v);if(v[0])setVenueId(v[0].id)}).catch((e:any)=>setError(e.message))},[slug]);useEffect(()=>{if(venueId&&date)publicApi.slots(slug,venueId,date).then(s=>{setSlots(s);setSlot('')})},[slug,venueId,date]);const venue=useMemo(()=>venues.find(v=>v.id===venueId),[venues,venueId]);if(done)return <div className="public-page"><div className="success-screen"><CheckCircle2 size={56}/><h1>Reserva recebida!</h1><p>Seu horário foi registrado. O estabelecimento poderá confirmar a reserva e o pagamento.</p><button className="btn primary" onClick={()=>{setDone(false);setDate('');setSlot('')}}>Fazer outra reserva</button></div></div>;return <div className="public-page" style={{'--tenant':tenant?.primaryColor||'#2563eb'} as any}><section className="public-hero" style={tenant?.coverUrl?{backgroundImage:`linear-gradient(90deg,rgba(7,13,24,.9),rgba(7,13,24,.45)),url(${tenant.coverUrl})`}:undefined}><div className="public-container">{tenant?.logoUrl&&<img className="tenant-logo" src={tenant.logoUrl}/>}<h1>{tenant?.name||'Agendamento online'}</h1><p><MapPin size={17}/>{tenant?.address||'Escolha seu espaço e reserve em poucos passos.'}</p></div></section><main className="public-container booking-flow">{error&&<div className="alert danger">{error}</div>}<section><div className="section-label">1. Escolha o espaço</div><div className="public-venues">{venues.map(v=><button key={v.id} className={venueId===v.id?'public-venue selected':'public-venue'} onClick={()=>setVenueId(v.id)}><span className="venue-icon"><CalendarDays/></span><strong>{v.name}</strong><small>{v.description||v.type}</small><b>{money(v.price)}</b><span>{v.durationMinutes} min</span></button>)}</div></section><section className="public-grid"><div><div className="section-label">2. Data</div><input className="date-input" type="date" min={new Date().toISOString().slice(0,10)} value={date} onChange={e=>setDate(e.target.value)}/><div className="section-label mt">3. Horário disponível</div><div className="slots">{!date?<p className="muted">Selecione uma data.</p>:slots.length?slots.map(s=><button key={s} className={slot===s?'slot selected':'slot'} onClick={()=>setSlot(s)}><Clock size={15}/>{new Date(s).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</button>):<p className="muted">Nenhum horário disponível nesta data.</p>}</div></div><div className="booking-box"><div className="section-label">4. Seus dados</div><form onSubmit={async e=>{e.preventDefault();if(!venueId||!slot)return;setError('');try{await publicApi.book(slug,{venueId,...form,startDateTime:slot});setDone(true)}catch(e:any){setError(e.message)}}}><Field label="Nome"><input required value={form.customerName} onChange={e=>setForm({...form,customerName:e.target.value})}/></Field><Field label="WhatsApp / telefone"><input required value={form.customerPhone} onChange={e=>setForm({...form,customerPhone:e.target.value})}/></Field><Field label="E-mail (opcional)"><input type="email" value={form.customerEmail} onChange={e=>setForm({...form,customerEmail:e.target.value})}/></Field><div className="summary"><span>{venue?.name||'Espaço'}</span><strong>{venue?money(venue.price):'—'}</strong></div><button disabled={!slot} className="btn tenant-btn full">Solicitar reserva</button></form></div></section></main><footer className="public-footer">Agendamento online · {tenant?.name}</footer></div>}
