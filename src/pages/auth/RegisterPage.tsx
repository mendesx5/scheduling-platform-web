import {useMemo,useState} from 'react';
import {Link,useNavigate,useSearchParams} from 'react-router-dom';
import {authApi} from '../../api/services';
import {Field,money} from '../../components/ui';
import {PLANS} from '../../config/plans';
import type {Plan} from '../../types';
import {Check,ChevronLeft} from 'lucide-react';
export default function RegisterPage(){const nav=useNavigate();const [params]=useSearchParams();const initial=(params.get('plan') as Plan)||'STARTER';const [plan,setPlan]=useState<Plan>(PLANS.some(p=>p.value===initial)?initial:'STARTER');const [form,setForm]=useState({name:'',slug:'',ownerName:'',email:'',password:'',phone:'',address:''});const [error,setError]=useState('');const selected=useMemo(()=>PLANS.find(p=>p.value===plan)!,[plan]);const submit=async(e:any)=>{e.preventDefault();setError('');try{await authApi.register(form);localStorage.setItem('needs_onboarding','true');localStorage.setItem('desired_plan',plan);nav('/login?registered=1')}catch(e:any){setError(e.message)}};return <div className="auth-page register-page"><div className="register-shell"><div className="register-side"><Link to="/" className="back-link light"><ChevronLeft/> Voltar</Link><div className="lp-brand light">Agenda<span>Hub</span></div><h2>Comece a organizar suas reservas.</h2><p>Crie sua conta e configure seu primeiro espaço em poucos minutos.</p><div className="register-benefits"><span><Check/> Página própria para reservas</span><span><Check/> Painel completo</span><span><Check/> Sem instalação</span><span><Check/> Experiência mobile</span></div></div><div className="register-form"><h1>Crie seu negócio</h1><p>Escolha o plano e informe seus dados.</p><div className="register-plans">{PLANS.map(p=><button type="button" className={p.value===plan?'register-plan selected':'register-plan'} onClick={()=>setPlan(p.value)} key={p.value}><b>{p.label}</b><span>{money(p.price)}/mês</span><small>{p.maxVenues} espaço{p.maxVenues>1?'s':''}</small></button>)}</div><div className="selected-plan-note">Plano <b>{selected.label}</b> selecionado. A cobrança automática será conectada na etapa de backend.</div><form onSubmit={submit} className="form-grid"><Field label="Estabelecimento"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Arena Central"/></Field><Field label="URL pública"><div className="slug-input"><span>/</span><input required pattern="[a-z0-9-]+" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,'')})} placeholder="arena-central"/></div></Field><Field label="Seu nome"><input required value={form.ownerName} onChange={e=>setForm({...form,ownerName:e.target.value})}/></Field><Field label="E-mail"><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Senha"><input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field><Field label="Telefone"><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></Field><div className="span-2"><Field label="Endereço"><input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></Field></div>{error&&<div className="alert danger span-2">{error}</div>}<button className="btn primary span-2 big">Criar minha conta</button></form><small className="auth-switch">Já possui conta? <Link to="/login">Entrar</Link></small></div></div></div>}
