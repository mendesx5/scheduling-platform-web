import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {authApi} from '../../api/services';import {useAuth} from '../../contexts/AuthContext';import {Field} from '../../components/ui';
export default function PlatformLoginPage(){const nav=useNavigate();const {setPlatformToken}=useAuth();const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');return <div className="auth-page platform-bg"><div className="auth-card"><div className="brand dark">Agenda<span>Hub</span></div><h1>Platform Admin</h1><form onSubmit={async e=>{e.preventDefault();try{const r=await authApi.platformLogin(form);setPlatformToken(r.token);nav('/platform')}catch(e:any){setError(e.message)}}}><Field label="E-mail"><input type="email" required onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Senha"><input type="password" required onChange={e=>setForm({...form,password:e.target.value})}/></Field>{error&&<div className="alert danger">{error}</div>}<button className="btn primary full">Entrar</button></form></div></div>}
