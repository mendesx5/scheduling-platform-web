import {useState} from 'react';
import {Link,useNavigate,useSearchParams} from 'react-router-dom';
import {authApi} from '../../api/services';
import {useAuth} from '../../contexts/AuthContext';
import {Field} from '../../components/ui';
import {ChevronLeft} from 'lucide-react';
export default function LoginPage(){const nav=useNavigate();const [params]=useSearchParams();const {setTenantToken}=useAuth();const [form,setForm]=useState({tenantSlug:'',email:'',password:''});const [error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();setError('');try{const r=await authApi.login(form);setTenantToken(r.token);const needs=localStorage.getItem('needs_onboarding')==='true';nav(needs?'/app/onboarding':'/app')}catch(e:any){setError(e.message)}};return <div className="auth-page"><div className="auth-card"><Link className="back-link" to="/"><ChevronLeft/> Início</Link><div className="brand dark">Agenda<span>Hub</span></div><h1>Entrar no painel</h1><p>Acesse a gestão do seu estabelecimento.</p>{params.get('registered')&&<div className="alert success">Conta criada. Entre para concluir a configuração.</div>}<form onSubmit={submit}><Field label="URL / slug do estabelecimento"><input required value={form.tenantSlug} onChange={e=>setForm({...form,tenantSlug:e.target.value})} placeholder="arena-nova-cruz"/></Field><Field label="E-mail"><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Senha"><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field>{error&&<div className="alert danger">{error}</div>}<button className="btn primary full">Entrar</button></form><small>Ainda não tem conta? <Link to="/register">Criar estabelecimento</Link></small></div></div>}
