import {useEffect,useState} from 'react';import {platformApi} from '../../api/services';import type {PlatformMetrics} from '../../types';import {Card,money} from '../../components/ui';
export default function PlatformDashboard(){const [m,setM]=useState<PlatformMetrics>();useEffect(()=>{platformApi.metrics().then(setM)},[]);return <><div className="page-head"><div><h1>Visão global</h1><p>Indicadores gerais da plataforma.</p></div></div><div className="stats"><Card><span>Tenants</span><strong>{m?.tenants??'—'}</strong></Card><Card><span>Ativos</span><strong>{m?.activeTenants??'—'}</strong></Card><Card><span>Reservas</span><strong>{m?.bookings??'—'}</strong></Card><Card><span>Volume bruto</span><strong>{money(m?.grossBookingValue||0)}</strong></Card></div></>}
